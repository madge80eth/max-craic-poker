# Upstash Redis (existing)
UPSTASH_REDIS_REST_URL=https://your-redis-instance.upstash.io
UPSTASH_REDIS_REST_TOKEN=your_token_here

# Vercel Cron Authentication (NEW - for automated tournaments)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
CRON_SECRET=your_random_secret_here

# Tipping Wallet Address (receives USDC tips on Base)
NEXT_PUBLIC_TIP_WALLET_ADDRESS=your_wallet_address_here

# Add other existing env vars below...
